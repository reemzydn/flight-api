apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flight-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: flight-api.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flight-api
                port:
                  number: 5000